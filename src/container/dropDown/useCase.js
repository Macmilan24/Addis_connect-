export default function UseCase({ onHover, onLeave }) {
  return (
    <div
      onMouseEnter={onHover}
      onMouseLeave={onLeave}
      className="h-30 w-full bg-slate-500"
    >
      dsad
    </div>
  );
}
